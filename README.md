# phoenix

On-chain orderbook that operates without a crank

### Build

To build the contract, run:

```
cd program
./build.sh
```

### Testing

To test the contract, run:

```
cd programs
./test.sh
```